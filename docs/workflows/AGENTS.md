# Workflows Documentation DOX

## Purpose

- Own durable task and goal workflow documentation plus workspace-owner persistent instructions.

## Local Contracts

- Keep the finite-task and multi-round-goal distinction, queue/claimed statuses, phase leases and crash recovery, controlled phase-owned dead-end recovery, runtime `## Progress` journaling, task-goal round links, waiting wakeups, and independent review semantics consistent across CLI, configuration, integrations, architecture, and README documentation.
- Document the five fixed role files under `.spynel/instructions/`, their precedence, fresh loading, safe file constraints, memory-edit behavior, and the content-free inspection command distinctly from prompts, DOX, workflows, history, and harness configuration.
- Document direct agent-owned transition notifications, agent-decided heartbeat reminders without framework reminder policy/state, and both explicit-origin and recent-authorized `spynel notify` behavior.
- Task-agent notification commands use concrete absolute `--workdir`, exact authorized `--origin`, and ordinary `--message` arguments; placeholders and stdin composition are not task-agent guidance.

## Child DOX Index

No child DOX files.